# andrewstech.github.io

## Local development

- Run the static site locally: `npm run dev`
- Run Stackbit Visual Editor (latest CLI): `npm run stackbit:dev`

The project now uses `stackbit.config.js` (current Stackbit config format) and no longer uses the legacy in-page `stackbit-widget-init` script embed.
